#labels Featured
#sidebar TableOfContents

= User Guide =

==DHEV Validators==
|| *Validator* || *Params* || *Message* || *Description* || *Example* ||
|| AfterEL || value<br/>includeLimit<br/>message || validator.after || || ||
|| AssertEL || value<br/>message || validator.assert || || ||
|| BeforeEL || value<br/>includeLimit<br/>message || validator.before || || ||
|| DecimalMaxEL || value<br/>includeLimit<br/>message || validator.max || || ||
|| DecimalMinEL || value<br/>includeLimit<br/>message || validator.min || || ||
|| DecimalRangeEL || min<br/>max<br/>includeMin<br/>includeMax<br/>message || validator.range || || ||
|| DigitsEL || maxIntegerDigits<br/>minIntegerDigits<br/>maxFractionalDigits<br/>minFractionalDigits<br/>message|| validator.digits || || ||
|| LengthEL || min<br/>max<br/>includeMin<br/>includeMax<br/>message || validator.length || || ||
|| MaxEL || value<br/>includeLimit<br/>message || validator.max || || ||
|| MinEL || value<br/>includeLimit<br/>message || validator.min || || ||
|| PatternEL || regex<br/>flags<br/>message<br/> || validator.pattern || || ||
|| RangeEL || min<br/>max<br/>includeMin<br/>includeMax<br/>message || validator.range || || ||
|| SizeEL || min<br/>max<br/>message || validator.size || || ||

==The DHEV sample application==
TODO move this on its own page
For example, enter 12 as age and click on register: you'll receive an error saying "User must be older than 18". Now scroll down and change the min age field from 18 to 12, then click on the button to update the configuration. Try again to use 12 as age: no error is returned this time!

The application has been tested on jboss 4 and 5, either with jdk 5 and 6, with no problems.

==DHEV, Seam and Hibernate validator==