#labels Featured
#sidebar TableOfContents

= User Guide =

==DHEV Validators==
|| *Validator* || *Params* || *Message* || *Description* || *Example* ||
|| AfterEL || value<br/>includeLimit<br/>message || validator.after || || ||
|| AssertEL || value<br/>message || || || ||
|| BeforeEL || value<br/>includeLimit<br/>message || || || ||
|| DecimalMaxEL || value<br/>includeLimit<br/>message || || || ||
|| DecimalMinEL || value<br/>includeLimit<br/>message || || || ||
|| DecimalRangeEL || min<br/>max<br/>includeMin<br/>includeMax<br/>message || || || ||
|| DigitsEL || maxIntegerDigits<br/>minIntegerDigits<br/>maxFractionalDigits<br/>minFractionalDigits<br/>message|| || || ||
|| LengthEL || min<br/>max<br/>includeMin<br/>includeMax<br/>message || || || ||
|| MaxEL || value<br/>includeLimit<br/>message || || || ||
|| MinEL || value<br/>includeLimit<br/>message || || || ||
|| PatternEL || regex<br/>flags<br/>message<br/> || || || ||
|| RangeEL || min<br/>max<br/>includeMin<br/>includeMax<br/>message || || || ||
|| SizeEL || min<br/>max<br/>message || || || ||

==The DHEV sample application==
TODO move this on its own page
For example, enter 12 as age and click on register: you'll receive an error saying "User must be older than 18". Now scroll down and change the min age field from 18 to 12, then click on the button to update the configuration. Try again to use 12 as age: no error is returned this time!

The application has been tested on jboss 4 and 5, either with jdk 5 and 6, with no problems.

==DHEV, Seam and Hibernate validator==